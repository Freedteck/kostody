import PageStub from "./PageStub";

const Product = () => {
  return (
    <PageStub
      eyebrow="Product"
      title="Every repair, on"
      accent="the record."
      lede="The full product tour is being built next: what engineers capture, what customers authorize, and how the tamper-proof timeline holds both sides to the same story."
    />
  );
};

export default Product;
