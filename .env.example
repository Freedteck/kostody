# Absolute base URL of the Kostody product app (engineer + customer apps).
# The site footer uses this to link visitors to the app. No trailing slash.
# Leave unset for local dev (links fall back to relative paths); set this to the
# deployed product domain in production, e.g. https://app.kostody.com
VITE_APP_URL=
