# Base URL of the Kostody backend API (include the /api suffix, no trailing slash).
# When unset, the app falls back to http://localhost:5000/api (see src/services/api.js).
VITE_API_URL=http://localhost:5000/api
